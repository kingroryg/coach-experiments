FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    psutil==6.1.1 \
    'llama-cpp-python[server]>=0.3.0'

# Expose server port
EXPOSE 8080

# Will be overridden by docker run command
CMD ["python", "-m", "llama_cpp.server"]
